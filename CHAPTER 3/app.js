// Q # 1
// // Declare a variable called age and assign your age to it
// var age = 25; // Replace 25 with your actual age

// // Display your age in an alert box
// alert("My age is: " + age);

//  Q # 2


        // // Check if localStorage supports
        // if (typeof(Storage) !== "undefined") {
        //     // Check if the visitor count is already stored in localStorage
        //     if (localStorage.visitorCount) {
        //         // Increment the visitor count if already stored
        //         localStorage.visitorCount = Number(localStorage.visitorCount) + 1;
        //     } else {
        //         // Initialize the visitor count to 1 if not stored previously
        //         localStorage.visitorCount = 1;
        //     }
        //     // Display the visitor count on the web page
        //     document.write("You have visited this site " + localStorage.visitorCount + " times.");
        // } else {
        //     // Display a message if localStorage is not supported
        //     document.write("Sorry, your browser does not support Web Storage. Visitor count cannot be tracked.");
        // }

        // Q # 3

        // Declare a variable called birthYear and assign your birth year to it


        // var birthYear = 1990; // Replace 1990 with your actual birth year

        // // Display a message showing your birth year in the browser
        // document.write("My birth year is: " + birthYear);

        // Q # 4 

        var visitorName = "ALI";
        var productTitle = "T-shirt";
        var quantity = 5;

        // Display the message in the browser
        document.write(visitorName + " ordered " + quantity + " " + productTitle + "(s) on XYZ Clothing store.");

